{
  "name": "tomcat-api-boilerplate",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "root": "apps/tomcat-api-boilerplate",
  "projectType": "application",
  "targets": {
    "prepare-env": {
      "dependsOn": [
        {
          "projects": ["jakarta-base-rest"],
          "target": "build"
        }
      ],
      "executor": "nx:run-commands",
      "options": {
        "command": "mkdir -p dist/apps/tomcat-api-boilerplate/wars && cp -r dist/libs/jakarta-base-rest/* dist/apps/tomcat-api-boilerplate/wars && cp -r apps/tomcat-api-boilerplate/* dist/apps/tomcat-api-boilerplate"
      }
    },
    "build": {
      "dependsOn": ["prepare-env"],
      "executor": "nx:run-commands",
      "options": {
        "command": "docker buildx build -t gipo355/tomcat-api-boilerplate -f Dockerfile . --load",
        "cwd": "dist/apps/tomcat-api-boilerplate"
      }
    },
    "compose": {
      "executor": "nx:run-commands",
      "options": {
        "command": "docker-compose -f compose.dev.yml up --no-recreate --watch",
        "cwd": "apps/tomcat-api-boilerplate"
      }
    },
    "serve": {
      "dependsOn": ["build"],
      "executor": "nx:run-commands",
      "options": {
        "command": "docker run --network=host gipo355/tomcat-api-boilerplate"
      }
    }
  }
}
